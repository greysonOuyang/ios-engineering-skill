# Scripts

## Swift risk scan

```bash
python3 scripts/scan_swift_patterns.py /path/to/project
```

The scanner reports candidates such as swallowed errors, fallback defaults, real-time dependencies, lifecycle reload points, and sleeps. A match is not a confirmed bug and must be evaluated through the selected skill mode.
